/// <references types="houdini-svelte">

/** @type {import('houdini').ConfigFile} */
const config = {
	watchSchema: {
		url: 'https://cms.wpdecoupled.dev/graphql',
	},
	plugins: {
		'houdini-svelte': {},
	},
};

export default config;
